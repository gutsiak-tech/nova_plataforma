# Dashboard

Frontend React + TypeScript + Vite do Observatório de Migrantes.

Setup completo: [`docs/development.md`](../docs/development.md).

```bash
cd dashboard
npm install
npm run dev
```

API em `http://127.0.0.1:8000` (proxy `/api` no Vite). ICTT v2.0 em `/ict`; ICT V1 em `/ict-v1`.
