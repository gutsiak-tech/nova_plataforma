/** Teto de `limit` aceito por `GET /api/gold/v1/table/{base_name}` (app/api/routes_gold.py). */
export const API_TABLE_MAX_LIMIT = 2000

/** Limite usado na consulta territorial de municípios (TerritoryPage). */
export const TERRITORY_TABLE_LIMIT = API_TABLE_MAX_LIMIT
