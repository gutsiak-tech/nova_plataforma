/** Ano padrão alinhado ao DEFAULT_ANO do backend (.env). Fallback temporário. */
export const DEFAULT_API_ANO = 2026
/** Mês padrão alinhado ao DEFAULT_MES do backend (.env). Fallback temporário. */
export const DEFAULT_API_MES = 4
